namespace LogoDesignPortal.Domain.Enums;

/// <summary>
/// Type of entity the notification references (Order, Invoice, Message, System).
/// </summary>
public enum NotificationReferenceType
{
    Order = 1,
    Invoice = 2,
    Message = 3,
    System = 4
}
