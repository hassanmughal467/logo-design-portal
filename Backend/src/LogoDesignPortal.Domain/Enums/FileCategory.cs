namespace LogoDesignPortal.Domain.Enums;

public enum FileCategory
{
    Source = 1,      // Source files (AI, PSD, etc.)
    Print = 2,      // Print-ready files (PDF, high-res PNG)
    Web = 3,        // Web-optimized files (PNG, SVG, JPG)
    Embroidery = 4  // Embroidery files
}
