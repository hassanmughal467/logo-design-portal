{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "IncludeExceptionDetailsInProduction": true,
  "ConnectionStrings": {
    "_comment": "Placeholder — override via the ConnectionStrings__DefaultConnection environment variable on the Staging IIS App Pool. App fails fast at startup if this placeholder is still in effect.",
    "DefaultConnection": "Server=localhost;Database=LogoDesignPortalDb_Staging;User=root;Password=REPLACE_WITH_ACTUAL_PASSWORD;"
  },
  "Jwt": {
    "_comment": "Placeholder (deliberately under 32 chars) — override via the Jwt__Key environment variable on the Staging IIS App Pool. Use a different secret than Production.",
    "Key": "SET_VIA_ENV_VAR_JWT__KEY",
    "Issuer": "LogoDesignPortal",
    "Audience": "LogoDesignPortalUsers"
  },
  "FileStorage": {
    "Path": "Files"
  },
  "ProductionSafety": {
    "DisableBillingGeneration": true,
    "DisableDesignerPayout": true,
    "DisableFileUploads": false,
    "DisableInvoiceEditing": false
  }
}
